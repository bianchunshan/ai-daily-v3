{
  "name": "ai-daily-v3",
  "version": "1.0.0",
  "private": true,
  "description": "Android wrapper for AI Daily",
  "scripts": {
    "android:sync": "cap sync android",
    "android:open": "cap open android",
    "android:debug": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}
